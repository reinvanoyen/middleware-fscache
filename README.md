## PSR-15 middleware for response caching on the filesystem
### !!! In this stage it only works easily with Oak framework

#### Installation

```ssh
composer require reinvanoyen/middlewares-fscache
```